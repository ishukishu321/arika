Only scripts placed directly in this folder can be run via the `run_script`
chat command (`.py`, `.bat`, `.ps1`, `.sh`). Arbitrary paths outside this
folder are not allowed — this is the safety boundary.
